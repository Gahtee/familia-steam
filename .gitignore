# dados de execução — banco, pid/ports/logs do serviço
data/
*.db
*.db-shm
*.db-wal
*.db-journal
*.log

# dependências (o projeto usa só Node embutido, mas se um dia houver)
node_modules/
frontend-novo/node_modules/
frontend-novo/dist/
frontend-novo/.output/
frontend-novo/.tanstack/

# build publicado: public/ é gerado pelo build do frontend-novo
# (mantido fora do git; gere com: npm run build em frontend-novo + cópia)
public/assets/
public/index.html
public/robots.txt
public/favicon.ico
!public/.gitkeep

# segredos / ambiente / infra local — NUNCA commitar
.env
.env.*
*.pem
*.key
credentials.json
config.yml
config.yaml
*tunnel*.json
.cert/
.cloudflared/

# SO / editores
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~
